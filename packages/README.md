# Packages

Shared libraries live here. Planned modules:

- `config`: linting, formatting, TypeScript presets.
- `types`: cross-service domain types.
- `utils`: shared helpers and adapters.
- `db`: Prisma schema and client generation.
- `ui`: reusable component library.

